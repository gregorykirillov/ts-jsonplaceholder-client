import React from 'react';

import { TodoList } from '~/src/components';

const TodoPage = () => {
    return (
        <>
            <h2>TodoPage</h2>

            <TodoList />
        </>
    );
};

export default TodoPage;
